{  
    "rewrites": [{ "source": "/uploads/profileImage/:path*", "destination": "/uploads/profileImage/:path*" }],
      "builds": [
        {
            "src": "./index.js",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src": "/(.*)",
            "dest":"/"
        }
    ]
}
